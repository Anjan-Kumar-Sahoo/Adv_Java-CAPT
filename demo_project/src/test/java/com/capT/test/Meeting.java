package com.capT.test;

public class Meeting {
	public static void main(String[] args) {
		
//		String s = "aabbcddeffg";
//		Optional<Character> ans = s.chars().mapToObj(c->(char)c).collect(Collectors.groupingBy( Function.identity(),LinkedHashMap::new,Collectors.counting() ))
//				.entrySet().stream().filter(e->e.getValue()==1).map(Map.Entry::getKey).skip(1).findFirst();
//		System.out.println(ans);
	}
}
